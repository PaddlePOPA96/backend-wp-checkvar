{
  "scripts": {
    "start": "node server.js",
    "vercel-build": "echo \"skip build; functions only\"",
    "seed:firestore": "node seed-firestore.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.9.0",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "firebase": "^12.6.0",
    "firebase-admin": "^12.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
